"""Runnable examples shipped inside the installed package.

One command boots a living world from a plain install. The repository's
``examples/`` tree carries the broader example tier; this package carries
the visual example, whose window renderer rides the distribution's
opt-in visual extra.
"""
